# main_code.py


def add_numbers(x, y): # Parameter names should follow 'snake_case' and be more descriptive.
    """
    Docstrings should be added at the module and function level to provide descriptions.
    """
    return x + y
